

eureka:
  instance:
#    prefer-ip-address: true
    # IP address with which this service will register on the Eureka server will be "localhost"
#    ip-address: localhost

  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
